version: "3.9"

services:
  rca-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      LLM_PROVIDER: template
      LOG_LEVEL: INFO
    volumes:
      - ./app/data:/app/app/data
    restart: unless-stopped
